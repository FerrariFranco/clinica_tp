import { HighlightActiveButtonDirective } from './highlight-active-button.directive';

describe('HighlightActiveButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightActiveButtonDirective();
    expect(directive).toBeTruthy();
  });
});
