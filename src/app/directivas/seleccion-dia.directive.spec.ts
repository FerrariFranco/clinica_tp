import { SeleccionDiaDirective } from './seleccion-dia.directive';

describe('SeleccionDiaDirective', () => {
  it('should create an instance', () => {
    const directive = new SeleccionDiaDirective();
    expect(directive).toBeTruthy();
  });
});
