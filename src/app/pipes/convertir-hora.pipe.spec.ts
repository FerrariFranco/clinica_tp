import { ConvertirHoraPipe } from './convertir-hora.pipe';

describe('ConvertirHoraPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertirHoraPipe();
    expect(pipe).toBeTruthy();
  });
});
